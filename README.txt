# Simple Hearthstone copycat game

Stack: Express

Do not forget to get all dependancies with: npm i
To start up the app you must first create the Database. 
    To do so: mysql -u root < db.sql
Then the server can be started with: node index
